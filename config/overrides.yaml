overrides_api_version: 1.0.0
selections:
  - export_kind: agent_studio_prompt
    name: system
    package_id: agr.alliance
    reason: Use the Alliance curator prompt when the Alliance package is installed.
