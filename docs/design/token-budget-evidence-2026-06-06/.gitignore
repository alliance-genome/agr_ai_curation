# Local Incus smoke rerun artifacts. These can include bulky logs, trace ids,
# and generated harness JSON that should be reviewed locally instead of
# committed with the design note.
validation-rerun-*/
