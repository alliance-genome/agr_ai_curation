# Core dependencies
fastapi
uvicorn[standard]
python-multipart  # Required for file uploads
pydantic>=2.0
pydantic-settings
python-dotenv>=1.2.2
pyyaml

# Authentication (AWS Cognito - Okta completely removed)
# RIP AND REPLACE complete: All Okta code deleted, including vendor/agr_fastapi_okta

# AWS Cognito Authentication
boto3>=1.34.0  # AWS SDK for Python (moved from AWS Services section)
fastapi-cognito>=1.0.0  # FastAPI integration for AWS Cognito
pyjwt[crypto]>=2.8.0  # JWT token handling and JWKS validation

# AI and Chat dependencies
openai
tiktoken  # Token preflight checks for embedding limits
openai-agents[litellm]==0.12.5  # Preserve the current Agents SDK integration while refreshing LiteLLM separately.
litellm>=1.83.0  # Patched after the 1.82.8 supply-chain incident and current Dependabot CVEs.
anthropic>=0.87.0  # Anthropic SDK for Opus chat in Prompt Explorer
aiohttp>=3.13.4  # Async HTTP client for PDFX service
langfuse>=4.0.0,<5.0.0  # Observability for LLM applications (v4 API: start_as_current_observation + propagate_attributes)

# Database dependencies
sqlalchemy>=2.0
psycopg2-binary
alembic
agr-abc-document-parsers>=1.5.0
agr-curation-api-client  # Use the published client release; keep lockfile current when updating

# Ontology processing
pronto  # OBO file parsing for biological ontologies

# Weaviate client - per weaviate-client 4.18.3 compatibility check:
# protobuf >= 6.30.0 requires grpcio >= 1.72.0
weaviate-client>=4.0
grpcio>=1.72.0  # Required for protobuf 6.x per weaviate-client compatibility matrix

# Redis for cross-worker state (stream cancellation)
redis>=5.0

# AWS Services (Feature 005-user-feedback-system SNS notifications)
# boto3>=1.34.0  # Moved to Authentication section for Cognito support

# PDF processing is done remotely by PDFX service (configure PDF_EXTRACTION_SERVICE_URL)

# CLI support
click

# Testing
pytest>=9.0.3
pytest-asyncio
pytest-cov
pytest-mock
httpx

# Development
black
flake8
mypy
numpy>=2.0.0
rapidfuzz>=3.14.3
