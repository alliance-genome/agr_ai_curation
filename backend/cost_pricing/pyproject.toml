[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agr-cost-pricing"
version = "1.0.0"
description = "Shared pure valuation of reviewed Langfuse model definitions"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
