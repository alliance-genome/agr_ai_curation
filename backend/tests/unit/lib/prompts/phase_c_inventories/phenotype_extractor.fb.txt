# Phase C FB-group render inventory: phenotype_extractor + FB group rules.
#
# Checked against the assembled render WITH the FB group
# (group_rules/fb.yaml) layered in: core + rewritten base prompt + FB group
# content. These are the load-bearing FB-specific phrases (the same kind
# test_phenotype_extractor_prompt_agent_and_group_rules_name_domain_contract
# asserts against the group rule, e.g. "validator resolve the final term").
#
# This task does NOT edit the group rules; the FB inventory exists to prove the
# base rewrite still renders cleanly UNDER the FB group and does not displace the
# group's load-bearing phenotype guidance. (PE-63 in
# docs/design/phaseC-checklists/phenotype_extractor.md.)

# FB ontology hint: preserve FBcv identifiers as validator hints
Preserve paper-supplied FlyBase Controlled Vocabulary (FBcv) identifiers as validator hints for Drosophila phenotype annotations.

# FB label-only -> let the active ontology validator resolve the final term
# (contract test asserts the substring "validator resolve the final term")
When the paper gives only a phenotype label, keep a label-backed candidate and let the active ontology validator resolve the final term.

# FB life-stage specificity guidance
Specify life stage when stated: "third instar larval lethality", "pupal lethality", "adult wing defect".

# FB phenotype vs disease distinction: human disease name is model context
The human disease name (e.g., Huntington's, Parkinson's) is disease model context, not a fly phenotype.
