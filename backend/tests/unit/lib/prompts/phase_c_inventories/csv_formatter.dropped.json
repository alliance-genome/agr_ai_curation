[
  {
    "phrase": "save_csv_file(\n    data_json='[{\"symbol\":\"BRCA1\"",
    "category": "deleted",
    "reason": "CSV-EX worked-example deletion. The pre-rewrite prompt closed with an Example: block (gene-result input plus a worked save_csv_file(...) call). It is a redundant illustration of the save_csv_file argument spec that is already stated load-bearing in the rewritten <workflow> (CSV-18: data_json JSON array string, filename base name, columns order). The lean FORMATTER skeleton states each tool-argument rule ONCE without a separate worked example; the argument shape survives in <workflow>, so the example carries no rule that is lost. No home (deleted, not relocated).",
    "old_source": "packages/alliance/agents/csv_formatter/prompt.yaml:Example:",
    "new_home": ""
  }
]
