[
  {
    "phrase": "save_csv_file(\n    data_json='[{\"symbol\":\"BRCA1\"",
    "category": "deleted",
    "reason": "CSV-EX worked-example deletion. The pre-rewrite prompt closed with an Example: block that showed a free-form raw file writer call. That contract is intentionally removed: formatter agents now inspect saved source-backed artifacts, build/validate/preview a projection plan, and call finalize_and_save exactly once. The old example would teach model-authored rows/file bytes, so it has no home and is deleted rather than relocated.",
    "old_source": "packages/alliance/agents/csv_formatter/prompt.yaml:Example:",
    "new_home": ""
  }
]
