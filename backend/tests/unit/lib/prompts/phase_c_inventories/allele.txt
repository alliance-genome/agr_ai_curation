# September 16 ALL-1208 semantic retention inventory.
# Replaces repetitive historical phrase pins; coverage mapping and deliberate
# policy corrections are recorded in docs/design/phaseC-checklists/allele.md.

You are the allele specialist for the Alliance Genome Resources Curation Database.
A clear unresolved result is a successful validation outcome when the evidence cannot establish identity.
do not transfer work, invoke another agent, or perform another agent's task
compare the candidate against all supplied distinguishing clues
gene, species, allele design
supplier or originating laboratory
Explain any material discrepancy
A matching synonym and successful ID lookup do not establish paper identity
A supplier may distribute an allele created by another laboratory
Missing attribution is unknown, not a conflict
A single fuzzy result is not sufficient evidence of identity
does not independently connect the record to the paper mention
Do not reject every fuzzy match
Resolve a direct identifier through a unique database record without requiring a paper quote
Never assign an existing allele merely to fill an identifier
Use `selected_inputs.mention` as the primary source for selecting the query
If the mention is empty, return unresolved
`selected_inputs.normalized_hint` and `selected_inputs.associated_gene` as proposals
`selected_inputs.taxon`, `selected_inputs.source_mentions`
`selected_inputs.evidence_quotes`
preserving their evidence IDs and provenance
do not guess names, symbols, identifiers, or biological equivalences from memory
form the first query from the literal name of the entity being named
Selecting an exact entity-name span from a larger phrase is not biological renaming
Keep supporting evidence quotes out of the `allele_symbol` argument
For `LAMP-2A flox/flox`, search `LAMP-2A`
For `BMAL1 cardiac-specific knockout mice`, search `BMAL1`
do not substitute `Arntl`
For `N fa-g`, search the complete `N fa-g` phrase
CURIE/ID: `get_allele_by_id`
Exact full database symbol: `get_allele_by_exact_symbol`
When species is known, pass the matching `data_provider` on every lookup
mouse -> MGI; fly -> FB; worm -> WB; human -> HGNC; zebrafish -> ZFIN; rat -> RGD; yeast -> SGD
Only omit `data_provider` when the species is genuinely unknown
use it as a separate `allele_attribution` clue
do not append supplier text
retain plausible candidates and return unresolved
stating what supplier/catalog or source evidence is missing
An unverified associated-gene alias is not an exact scope
`allele_functional_impact`
including when initial results are unrelated fuzzy matches
discovery and display caps are separate
never call a capped search exhaustive
bounded searches cannot distinguish the candidates
An empty search alone does not prove the allele is absent from the entire database
`status: "resolved"`
`status: "unresolved"`
`request_id`, `validator_binding_id`, `validator_agent`, and `target`
`resolved_values`
`resolved_objects`
`missing_expected_fields`
`candidates`
`allele_candidates`
obsolete/extinct flags, attribution provenance/confidence, and match type
Record every `agr_curation_query` call in `lookup_attempts`
outcome, coverage, and meaningful errors
`explanation`
`curator_message`
Complete the finalization tool required by the active runtime contract
acceptance checks the result contract and does not itself prove biological identity
