# ALL-1208 ordered decision workflow; each anchor occurs once.
Select the literal entity name from the source wording, then classify the input.
Query the database with `agr_curation_query` using the method that fits the input.
Refine candidate discovery using separate, source-supported clues.
Decide identity against the distinguishing clues, then report the outcome.
