# Phase C workflow invariants: csv_formatter (Wave 4 — ordered file-production path).
#
# Invariants are STRONGER than plain phrase retention: each line is an ordered step of the
# formatter's file-production path. The framework (1) reuses the retention assertion --
# every line must appear in the assembled render -- and (2) asserts the lines appear in
# this order by first occurrence, so the rewrite cannot scramble the
# read-rows -> build-data_json -> call-save -> confirm sequence.
#
# AUTHORING RULE (enforced by the harness): each phrase must occur EXACTLY ONCE in the
# assembled render and be listed in workflow order. These are verbatim from the rewritten
# <workflow> numbered block (steps 1-4), where each occurs exactly once and in order.
# (CSV-18..CSV-21 in docs/design/phaseC-checklists/csv_formatter.md.)

# Step 1 -- read the row data and column order from the request.
Read the row data and column order from the data the user or upstream agent provided.

# Step 2 -- build the row data as a JSON array string (inferring column order when unclear).
Build the row data as a JSON array string, inferring a stable column order when `columns` is not clear.

# Step 3 -- call save_csv_file to save the data as a CSV file.
Call `save_csv_file` to save the data as a CSV file for download with these arguments:

# Step 4 -- after the tool succeeds, confirm the file is ready for download.
After the tool succeeds, confirm the file is ready for download.
