[
  {
    "phrase": "Return a `GOAnnotationsResult`.",
    "synonyms": [
      "produce JSON matching GOAnnotationsResult; the structured-output layer below is authoritative for final response shape"
    ],
    "category": "relocated",
    "reason": "GOA-OUT output-mandate de-dup. The pre-rewrite '# Output' opened with 'Return a `GOAnnotationsResult`.' and listed the required shared validator fields as a JSON-only output mandate that the locked Generated Runtime Contract already injects ('Output contract from agent.yaml: produce JSON matching GOAnnotationsResult; the structured-output layer below is authoritative for final response shape' plus the CRITICAL structured-output block 'Your final response MUST be valid JSON matching the GOAnnotationsResult schema EXACTLY'). The lean rewrite no longer restates the JSON-only output mandate; the core half of the render owns it. The home check is satisfied by the synonym (the core's injected output-contract line). The `GOAnnotationsResult` token is KEPT once in <goal>, and the GO-annotation root-field detail is KEPT in <result_contract> because the core does not enumerate it.",
    "old_source": "packages/alliance/agents/go_annotations/prompt.yaml:# Output",
    "new_home": "render"
  }
]
