[
  {
    "phrase": "You MUST call the relevant package tool before marking any present ontology, chemical (ChEBI as ontology term), data-provider, or taxon component as resolved.",
    "synonyms": [
      "call at least one of agr_curation_query before final output"
    ],
    "category": "relocated",
    "reason": "ECV-RTC / ECV-06 de-dup. The pre-rewrite '# Lower-Level Capabilities' restated the required-tool-call imperative that the locked Generated Runtime Contract already injects ('Required tool-call policy: call at least one of agr_curation_query before final output'). The lean rewrite drops the literal machine MUST-imperative and folds the curator-facing grounding rule into <resolution_and_validation_rules> once ('call the relevant package tool before marking any present ontology, chemical (ChEBI as an ontology term), data-provider, or taxon component resolved'); the machine imperative survives in the core half of the assembled render. The literal `agr_curation_query` token is still present in the base prompt.",
    "old_source": "packages/alliance/agents/experimental_condition/prompt.yaml:# Lower-Level Capabilities",
    "new_home": "render"
  },
  {
    "phrase": "Validate an experimental-condition payload from a `DomainValidationRequest` and return structured `ExperimentalConditionValidationResult` data using the shared validator result contract.",
    "synonyms": [
      "produce JSON matching ExperimentalConditionValidationResult; the structured-output layer below is authoritative for final response shape"
    ],
    "category": "relocated",
    "reason": "ECV-OUT / ECV-02 output-mandate de-dup. The pre-rewrite '# Goal' carried the 'return structured ExperimentalConditionValidationResult data / shared validator result contract' output mandate that the locked core already injects ('Output contract from agent.yaml: produce JSON matching ExperimentalConditionValidationResult; the structured-output layer below is authoritative for final response shape' plus the CRITICAL structured-output block 'Your final response MUST be valid JSON matching the ExperimentalConditionValidationResult schema EXACTLY'). The lean rewrite no longer restates the JSON-only output mandate; the core half of the render owns it. The home check is satisfied by the synonym (the core's injected output-contract line). The exact pre-rewrite phrase '...from a `DomainValidationRequest` and return structured...' is preserved verbatim in the rewritten <goal> (now 'Validate ONE composite experimental-condition payload from a `DomainValidationRequest`...'), and the composite root-field detail (component_validations, normalized_components, condition_status, condition_id, unresolved_components) is KEPT in <result_contract> because the core does not enumerate it.",
    "old_source": "packages/alliance/agents/experimental_condition/prompt.yaml:# Goal",
    "new_home": "render"
  },
  {
    "phrase": "`condition_chemical_curie`, `condition_chemical_name`, or `chemical_name`: ChEBI chemical inputs, validated as ontology terms (ontologytermtype `CHEBITerm`) via `agr_curation_query` `get_ontology_term`.",
    "synonyms": [
      "`condition_chemical_curie`, `condition_chemical_name`, or `chemical_name`: ChEBI chemical inputs, validated as ontology terms (ontologytermtype `CHEBITerm`) via grouped `agr_curation_query` `get_ontology_terms`, with `get_ontology_term` only for a residual targeted lookup."
    ],
    "category": "relocated",
    "reason": "ALL-592 changes the condition-chemical guidance from single-term-first to grouped-first lookup so compatible CURIEs across a validator batch share one tool call. The single-term method remains only for a residual targeted lookup.",
    "old_source": "packages/alliance/agents/experimental_condition/prompt.yaml:<scope>",
    "new_home": "render"
  },
  {
    "phrase": "call `get_ontology_term` with `term=<CHEBI:...>`. Do NOT call `chebi_api_call` for condition chemicals; the ChEBI REST endpoint is not the resolution path for condition components.",
    "synonyms": [
      "Validate `condition_chemical_curie` via grouped `agr_curation_query` `get_ontology_terms`, exactly like ZECO/taxon; use `get_ontology_term` with `term=<CHEBI:...>` only when a residual targeted lookup is needed. Do NOT call `chebi_api_call` for condition chemicals; the ChEBI REST endpoint is not the resolution path for condition components."
    ],
    "category": "relocated",
    "reason": "ALL-592 preserves the ChEBI database-resolution rule while moving its primary path to the shared grouped ontology lookup. The prohibited chebi_api_call path is unchanged.",
    "old_source": "packages/alliance/agents/experimental_condition/prompt.yaml:<resolution_and_validation_rules>",
    "new_home": "render"
  },
  {
    "phrase": "For each present grounded component -- condition class/id, condition chemical (ChEBI ontology term), condition taxon, quantity/unit, data provider -- call the owning capability's `agr_curation_query` method before resolving it, and record each call in that component's `lookup_attempts`.",
    "synonyms": [
      "Group same-family lookup work across every target in the request before calling tools. Collect the unique present CURIE components -- condition class/id, condition chemical (ChEBI ontology term), and condition taxon -- and make one `agr_curation_query` `get_ontology_terms` call with the combined `terms` list before any single-term ontology lookup. Assign each returned record back to every matching component and record the shared call in that component's `lookup_attempts`.",
      "For each present grounded component not resolved by the grouped call -- including quantity/unit, data provider, or an ontology component needing targeted follow-up -- call only the owning capability's residual `agr_curation_query` method before resolving it, and record that call in the component's `lookup_attempts`."
    ],
    "category": "relocated",
    "reason": "ALL-592 splits the old per-component-first workflow step into a grouped cross-target lookup followed by residual per-component calls. Lookup-attempt provenance remains required for both paths.",
    "old_source": "packages/alliance/agents/experimental_condition/prompt.yaml:<lookup_workflow>",
    "new_home": "render"
  }
]
