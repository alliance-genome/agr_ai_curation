[
  {
    "phrase": "save_json_file(\n    data_json='[{\"symbol\":\"BRCA1\"",
    "category": "deleted",
    "reason": "JSON-EX worked-example deletion. The pre-rewrite prompt closed with a # Example block (gene-result input plus a worked save_json_file(...) call). It is a redundant illustration of the save_json_file argument spec that is already stated load-bearing in the rewritten <workflow> (JSON-12: data_json valid JSON string, filename base name, pretty flag). The lean FORMATTER skeleton states each tool-argument rule ONCE without a separate worked example; the argument shape survives in <workflow>, so the example carries no rule that is lost. No home (deleted, not relocated).",
    "old_source": "packages/alliance/agents/json_formatter/prompt.yaml:# Example",
    "new_home": ""
  }
]
