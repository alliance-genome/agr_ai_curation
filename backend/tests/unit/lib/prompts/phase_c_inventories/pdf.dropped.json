[
  {
    "phrase": "Returns JSON only, matching `PdfExtractionResultEnvelope`.",
    "category": "relocated",
    "reason": "PDF-08 (de-dup). The pre-rewrite <success_criteria> restated the JSON-only output mandate the locked core already injects. assembly.py::_build_core_generated_content emits both the 'Output contract from agent.yaml: produce JSON matching PdfExtractionResultEnvelope' line and the 'CRITICAL: ALWAYS PRODUCE STRUCTURED OUTPUT AS VALID JSON ... valid JSON matching the PdfExtractionResultEnvelope schema EXACTLY' block. The base prompt no longer restates 'Returns JSON only'; the fact survives in the core half of the assembled render. Verified the curation-specific output guidance (field roster, no Evidence/Citations/Sources headers, kept_count rules) is preserved in <output_contract>.",
    "old_source": "packages/alliance/agents/pdf/prompt.yaml:<success_criteria>",
    "new_home": "render",
    "synonyms": ["produce JSON matching PdfExtractionResultEnvelope", "valid JSON matching the PdfExtractionResultEnvelope schema EXACTLY"]
  },
  {
    "phrase": "Return JSON only, matching `PdfExtractionResultEnvelope`. Use the schema fields rather than adding extra prose-only sections.",
    "category": "relocated",
    "reason": "PDF-26 (de-dup). The pre-rewrite <output_contract> opener again restated the JSON-only mandate the locked core owns (the structured-output layer is authoritative for final response shape). The base prompt keeps the load-bearing half -- 'Populate the PdfExtractionResultEnvelope fields rather than adding extra prose-only sections' -- but drops the 'Return JSON only' restatement, which survives in the core half of the render.",
    "old_source": "packages/alliance/agents/pdf/prompt.yaml:<output_contract>",
    "new_home": "render",
    "synonyms": ["produce JSON matching PdfExtractionResultEnvelope", "Populate the `PdfExtractionResultEnvelope` fields rather than adding extra prose-only sections"]
  },
  {
    "phrase": "Use document retrieval tools before answering.",
    "category": "relocated",
    "reason": "PDF-22 (de-dup). The pre-rewrite <tools> 'Retrieval budget and stopping rules' restated the retrieval-before-output imperative the locked core already mandates as a required tool-call policy ('call at least one document retrieval tool (search_document, read_section, or read_subsection) before final output'). The base prompt no longer restates the imperative; the curator-facing success line 'You retrieved from the document before answering' (PDF-06, kept in <success_criteria>) and the core's required-tool-call policy together carry it.",
    "old_source": "packages/alliance/agents/pdf/prompt.yaml:<tools>",
    "new_home": "render",
    "synonyms": ["call at least one document retrieval tool (search_document, read_section, or read_subsection) before final output", "You retrieved from the document before answering"]
  },
  {
    "phrase": "search_document: document search across the paper; use default/auto for concepts and lexical-heavy modes for exact controlled tokens",
    "category": "relocated",
    "reason": "PDF-27. The per-tool capability prose for search_document (blended/auto default for concepts; lexical for exact controlled tokens; section restriction) lives in the search_document bindings.yaml description/summary on the production path. The Phase C skeleton relocates the descriptive tool prose to the tool description; the base <retrieval_strategy> keeps only the curation-relevant 'prefer lexical-heavy search modes for exact controlled tokens' hint.",
    "old_source": "packages/alliance/agents/pdf/prompt.yaml:<tools>",
    "new_home": "bindings:search_document",
    "synonyms": ["The default blended search combines meaning-based and exact-word matching", "Exact-word matching is best for specific gene symbols, strains, alleles, probes, or identifiers"]
  },
  {
    "phrase": "read_section: read a full top-level section such as Methods or Results; read_subsection: read a targeted subsection when you know where the answer should be",
    "category": "relocated",
    "reason": "PDF-28. The full-section/full-subsection coverage prose lives in the read_section/read_subsection bindings.yaml summaries ('complete text of every passage in a section, grouped by the paper's own structure rather than by page order ... full coverage'). The base <retrieval_strategy> keeps the curation hint to use read_section/read_subsection for full coverage of a known section, and the descriptive mechanics are relocated to the tool descriptions.",
    "old_source": "packages/alliance/agents/pdf/prompt.yaml:<tools>",
    "new_home": "bindings:read_section",
    "synonyms": ["complete text of every passage in a section, grouped by the paper's own structure rather than by page order", "full coverage"]
  },
  {
    "phrase": "search-context / search_infrastructure block",
    "category": "deleted",
    "reason": "PDF (search block). The pre-rewrite (HEAD b7e3a415) pdf prompt has NO <search_context>/<search_infrastructure> block: it was already removed in Phase B (commit 24144d52, 'drop duplicated search-infrastructure block from pdf base prompt (now in tool descriptions)'), and the 'Multiple span_ids -> one evidence record' sentence was relocated into <evidence_rules> then. Recorded as a deleted no-op entry for parity with the worked examples so the deletion ledger is explicit; nothing was present to remove in this Phase-C task.",
    "old_source": "packages/alliance/agents/pdf/prompt.yaml (already removed in Phase B)",
    "new_home": ""
  }
]
